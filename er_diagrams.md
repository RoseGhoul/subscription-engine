# Jio Subscription Engine - Entity Relationship Diagrams

Here are the high-level Entity Relationship Diagrams (ERDs) for the six TMF modules completed so far. These diagrams represent the core aggregate roots and their domain boundaries based on the TM Forum API specifications we implemented.

Since we are using a **Clean Architecture with strict module isolation**, relationships between modules are stored as *soft links* (References, usually stored as String IDs or HREFs) rather than hard foreign key database constraints.

---

## 1. TMF629 Customer Management
Manages the lifecycle of a Customer. A Customer represents a person or organization that buys products and services from the enterprise or receives free offers or services.

```mermaid
erDiagram
    CUSTOMER ||--o{ CONTACT_MEDIUM : has
    CUSTOMER ||--o{ CHARACTERISTIC : described_by
    CUSTOMER ||--o{ RELATED_PARTY : associated_with
    CUSTOMER ||--o{ ACCOUNT_REF : billed_to
    
    CUSTOMER {
        String id
        String name
        String status
        String statusReason
        DateTime validFor_startDateTime
        DateTime validFor_endDateTime
        String engagedParty_id
        String engagedParty_role
    }
    
    CONTACT_MEDIUM {
        String mediumType
        Boolean preferred
        String validFor
    }
    
    CHARACTERISTIC {
        String name
        String valueType
        String value
    }
    
    RELATED_PARTY {
        String id
        String name
        String role
        String referredType
    }
    
    ACCOUNT_REF {
        String id
        String name
        String description
    }
```

---

## 2. TMF669 Party Role Management
Manages roles played by a Party (Individual or Organization) in a given context (e.g., Billing Administrator, User, Technical Contact).

```mermaid
erDiagram
    PARTY_ROLE ||--o{ CONTACT_MEDIUM : has
    PARTY_ROLE ||--o{ CHARACTERISTIC : described_by
    PARTY_ROLE ||--o{ RELATED_PARTY : associated_with
    PARTY_ROLE ||--o{ ACCOUNT_REF : manages
    PARTY_ROLE ||--o{ PAYMENT_METHOD_REF : uses
    PARTY_ROLE ||--o{ CREDIT_PROFILE : evaluated_by
    
    PARTY_ROLE {
        String id
        String name
        String status
        String statusReason
        String engagedParty_id
        String engagedParty_role
    }
```

---

## 3. TMF620 Product Catalog Management
Defines the commercial products available for sale (Product Offerings) and their technical specifications (Product Specifications).

```mermaid
erDiagram
    CATALOG ||--o{ CATEGORY : categorizes
    CATEGORY ||--o{ PRODUCT_OFFERING : contains
    PRODUCT_OFFERING ||--o{ BUNDLED_PRODUCT_OFFERING : bundles
    PRODUCT_OFFERING ||--o{ PRODUCT_OFFERING_PRICE : priced_by
    PRODUCT_OFFERING ||--o{ PRODUCT_SPECIFICATION_REF : defined_by
    
    CATALOG {
        String id
        String name
        String catalogType
        String status
    }
    
    CATEGORY {
        String id
        String name
        String parentId
    }
    
    PRODUCT_OFFERING {
        String id
        String name
        Boolean isBundle
        String status
    }
    
    PRODUCT_OFFERING_PRICE {
        String id
        String name
        String priceType
        Number price_value
        String price_currency
    }
```

---

## 4. TMF622 Product Order Management
Manages the end-to-end lifecycle of a customer order for products.

```mermaid
erDiagram
    PRODUCT_ORDER ||--|{ PRODUCT_ORDER_ITEM : contains
    PRODUCT_ORDER ||--o{ RELATED_PARTY : placed_by
    PRODUCT_ORDER ||--o{ NOTE : annotated_with
    
    PRODUCT_ORDER_ITEM ||--o| PRODUCT_OFFERING_REF : orders
    PRODUCT_ORDER_ITEM ||--o| PRODUCT : configures
    PRODUCT_ORDER_ITEM ||--o| PRODUCT_ORDER_ITEM_PRICE : priced_at
    
    PRODUCT_ORDER {
        String id
        String externalId
        String state
        DateTime orderDate
        DateTime requestedCompletionDate
    }
    
    PRODUCT_ORDER_ITEM {
        String id
        String action
        String state
        Number quantity
    }
    
    PRODUCT {
        String id
        String place_role
        String characteristic_name
    }
```

---

## 5. TMF635 Usage Management
Records consumption metrics (Usage) generated by products and services for billing and analytics.

```mermaid
erDiagram
    USAGE ||--o{ USAGE_CHARACTERISTIC : measured_by
    USAGE ||--o{ RELATED_PARTY : generated_by
    USAGE ||--o| USAGE_SPECIFICATION_REF : conforms_to
    
    USAGE {
        String id
        String usageType
        DateTime usageDate
        String status
        String description
    }
    
    USAGE_CHARACTERISTIC {
        String name
        String valueType
        String value
    }
    
    USAGE_SPECIFICATION_REF {
        String id
        String name
    }
```

---

## 6. TMF637 Product Inventory Management
Tracks the actual instantiated products currently owned/subscribed to by a customer.

```mermaid
erDiagram
    PRODUCT ||--o{ PRODUCT_CHARACTERISTIC : configured_with
    PRODUCT ||--o{ PRODUCT_PRICE : incurs
    PRODUCT ||--o{ PRODUCT_RELATIONSHIP : relates_to
    PRODUCT ||--o{ RELATED_PLACE : installed_at
    PRODUCT ||--o{ RELATED_PARTY : owned_by
    
    PRODUCT {
        String id
        String name
        String productSerialNumber
        String status
        Boolean isBundle
        DateTime startDate
    }
    
    PRODUCT_CHARACTERISTIC {
        String name
        String valueType
        String value
    }
    
    PRODUCT_PRICE {
        String name
        String priceType
        Number price_value
    }
    
    PRODUCT_RELATIONSHIP {
        String relationshipType
        String product_id
    }
    
    RELATED_PLACE {
        String role
        String name
    }
```
